### 스패로우 엔터프라이즈 개발

DevSecOps 구축을 도와주는 웹 기반 Application (상시 업무)

- `Next` 기반의 웹 Application으로, 소프트웨어 **품질/보안 검증 제품의 프론트엔드 전반**을 담당하고 있습니다.
- **SSO / OTP(2FA) 인증 모듈**을 직접 구현하며, 보안에 민감한 로직과 복잡한 비동기 흐름을 설계했습니다.
- **GraphQL 요청 계층을 설계**하고, `React-Query`와 `Zustand`로 **서버 상태와 클라이언트 상태를 명확히 분리**했습니다.
- 금융권 고객사 등 **보안 요구사항이 높은 환경**의 개발 프로세스에 맞춰 제품을 개발하고 있습니다.
- 에러 바운더리 설계를 통해 장애 상황에서도 서비스 전체가 중단되지 않도록 방어했습니다.
- `TypeScript` `React` `Next` `React-Query` `Zustand` `GraphQL` `Emotion.js` `Storybook`

#### 타입마다 규격이 다른 업로드 폼을 선언형 설정으로 분리했습니다.

**2026.02 – 2026.03** · 신규 기능 리드

사용자가 선택한 **타입에 따라 필수 입력값·허용 확장자·파일 파싱 방식이 전부 달라지는** 등록 폼입니다.
지원 타입이 6종이고 각 타입의 규격이 서로 겹치지 않아, 단순 조건 분기로는 유지보수가 불가능한 구조였습니다.

**타입별 규격을 설정으로 분리** — 타입마다 필드 구성 자체가 다르므로, 분기문 대신 선언형 설정 테이블에서
렌더링·검증·전송 페이로드를 결정하도록 했습니다. 타입 추가 시 기존 코드를 건드리지 않고 확장할 수 있습니다.

```js
const TYPE_CONFIG = {
  typeA: {
    upload: { maxFiles: 2, accept: '.aa,.xml' },
    parser: {
      match: (fileName) => /\.(aa|xml)$/i.test(fileName),
      parse: async (file) => parseDocument(file),   // 문서 내부에서 식별자 추출
    },
  },
  typeB: {
    upload: { maxFiles: 1, accept: '.bb' },
    parser: {
      match: (fileName) => /\.bb$/i.test(fileName),
      parse: (file) => parseFileName(file.name),    // 파일명 규격에서 추출
    },
  },
  typeC: { upload: { maxFiles: 1, accept: '.cc' } }, // 파서 없음
};

const REQUIRED_FIELDS_BY_TYPE = {
  typeA: ['source', 'files', 'groupKey', 'itemKey', 'version'],
  typeB: ['source', 'files', 'name', 'version', 'variant'],
  typeC: ['source', 'files', 'name', 'version'],
};
```

**비동기 파싱 결과의 경합(race condition) 해결** — 업로드한 파일을 클라이언트에서 파싱해 폼을 자동 완성하는데,
파싱이 비동기라 사용자가 타입을 바꾸거나 파일을 다시 고르면 **이전 요청의 결과가 뒤늦게 도착해 현재 폼을 덮어썼습니다.**
요청마다 시퀀스 번호를 발급하고, 완료 시점에 자신이 최신인지 확인해 낡은 결과를 폐기합니다.

```js
const parseSequenceRef = useRef(0);

const handleFileChange = async (name, files) => {
  const sequence = ++parseSequenceRef.current;   // 이번 요청의 번호 선점
  setFormValue((prev) => ({ ...prev, [name]: files }));

  const parser = TYPE_CONFIG[selectedType]?.parser;
  const target = files?.slice().reverse().find((f) => parser?.match(f.name));
  if (!target) return resetParsedValue();

  const parsed = await parser.parse(target);

  // 파싱 도중 타입 변경·파일 재선택이 있었다면 이 결과는 이미 낡음 → 폐기
  if (sequence !== parseSequenceRef.current) return;

  setFormValue((prev) => ({ ...prev, ...parsed }));
};
```

**자동 완성값과 검증 상태의 충돌** — 파서가 채운 값은 "검증 통과"로 봐야 제출이 가능한데,
통과로 고정하면 사용자가 그 값을 수정해도 재검증이 돌지 않았습니다.
검증 상태를 3-state로 모델링해 두 요구를 동시에 만족시켰습니다.

```js
// true = 검증 통과 / false = 미통과 / 'dirty' = 통과 이력이 있으나 사용자가 수정함
const DIRTY = 'dirty';
const isPassed = (status) => status === true || status === DIRTY;

const getNextStatus = ({ previousStatus, value }) => {
  const hasValue =
    typeof value === 'string' ? value.trim().length > 0 : value != null;

  if (!hasValue) return false;                     // 비우면 무조건 미통과
  return isPassed(previousStatus) ? DIRTY : false; // 통과 이력이 있으면 dirty로 전이
};

const isSubmittable = ({ formValue, selectedType, validationState }) =>
  hasRequiredValues(formValue, selectedType) &&
  (REQUIRED_FIELDS_BY_TYPE[selectedType] ?? [])
    .every((field) => isPassed(validationState[field]));
```

**하위 폼 검증 집계** — 자식 폼들의 검증 핸들을 부모가 수집해 한 번에 실행합니다.
`Promise.all`이 아닌 `allSettled`를 써서 첫 실패에서 중단하지 않으므로, 사용자는 모든 오류를 한 번에 확인합니다.

```js
useImperativeHandle(ref, () => ({
  validate: () => {
    const handles = [
      sourceRef.current?.validate(),
      ...(config ? [uploadRef.current?.validate()] : []),
      fieldsRef.current?.validate(),
    ];
    return Promise.allSettled(handles).then((results) => {
      const rejected = results.filter((r) => r.status === 'rejected');
      if (rejected.length > 0) return Promise.reject(rejected[0].reason);
    });
  },
  getValue: () => buildPayloadByType(selectedType, formValue),
}));
```

문서 파일 파싱에서는 값이 변수로 선언되어 있거나 상위 문서에만 정의된 경우
빈 값이 들어오던 문제가 있어, 변수 치환과 상위 문서 상속 폴백을 추가했습니다.

```js
// 값이 ${key} 형태면 문서 내 선언부에서 실제 값을 찾아 치환
if (version?.startsWith('${') && version.endsWith('}')) {
  const key = version.slice(2, -1);
  version = findDeclaredValue(root, key) ?? null;
}

// 자기 자신에 없으면 상위(부모) 선언을 상속
const parent = getDirectChild(root, 'parent');
if (parent) {
  groupKey ??= getDirectChildValue(parent, 'groupKey');
  version  ??= getDirectChildValue(parent, 'version');
}
```

---

#### 오래 걸리는 서버 작업의 진행 상태를 필요할 때만 폴링하도록 했습니다.

**2026.04 – 2026.05**

사용자가 규칙을 정의해 저장하면 서버가 **오래 걸리는 재계산 작업**을 비동기로 수행하고,
UI는 그동안 진행 상태를 반영해야 하는 기능입니다.

**필요할 때만 폴링** — `refetchInterval`을 값이 아닌 **함수로 넘겨**, 응답 상태가 `진행 중`인 동안에만
재조회하고 완료되면 스스로 멈추게 했습니다. 상태 전이를 서버 응답이 직접 제어하므로 별도 타이머 관리가 없습니다.
**기능 게이팅**도 같은 곳에서 처리해, 권한이 없는 환경에서는 요청 자체가 나가지 않습니다.

```js
const hasFeature = licenses?.['FEATURE_KEY'];

const { data: taskConfig } = useQuery({
  queryKey: ['taskConfig', resourceId],
  queryFn: fetchTaskConfig(resourceId),
  enabled: !!resourceId && !!hasFeature,   // 미보유 환경에선 호출 자체를 차단
  refetchInterval: (query) =>
    query.state.data?.status === 'IN_PROGRESS' ? 3000 : false, // 끝나면 자동 정지
});
```

**고비용 작업의 불필요한 실행 방지** — 저장할 때마다 재계산을 돌리면 데이터가 많은 환경에서 비용이 큽니다.
깊은 비교로 실제 변경 여부를 판정하고, 세 조건이 모두 참일 때만 확인 모달을 띄웁니다.

```js
const hasChange =
  isEnabled !== savedEnabled || !isEqual(savedCriteria, criteria);

// 실제 변경 + 기능 활성화 + 계산할 데이터 존재 → 이때만 재계산이 의미 있음
const needsRecompute = hasChange && isEnabled && !isEmptyResource;

const handleSubmit = () => {
  if (needsRecompute) setConfirmModalVisible(true);
  else save.mutate();                       // 그 외에는 확인 없이 즉시 저장
};

// 안내 문구도 실제 수행된 작업에 맞춰 분기
const messageKey = needsRecompute
  ? 'msg.success.save.and.recompute'
  : 'msg.success.save';
```

**조합 폭발하는 렌더 분기의 정리** — 3개 상태의 조합으로 아이콘·툴팁·읽기 전용 여부가 모두 달라졌습니다.
JSX 안에 중첩되던 삼항 연산자를 파생값으로 끌어올려, 각 상태의 결과를 한눈에 읽히게 했습니다.

```js
const tooltipMessage = useMemo(() => {
  if (!hasFeature) return t('msg.feature.unavailable');
  if (!isEnabled)  return t('msg.not.configured');
  if (isComputing) return t('msg.computing');
  return t('msg.edit');
}, [hasFeature, isEnabled, isComputing]);

const buttonIcon = useMemo(() => {
  if (hasFeature && !isEnabled) return <Icon name="base" muted />;
  const badge = !hasFeature ? 'lock' : isComputing ? 'progress' : 'done';
  return <CombiIcon main="base" badge={badge} />;
}, [hasFeature, isEnabled, isComputing]);
```

---

#### 스키마가 제각각인 4종 이력을 어댑터로 정규화했습니다.

**2026.06 – 2026.07**

한 대상에 대한 모든 변경 이력을 시간순으로 보여주는 감사(audit) 화면입니다.
이력 종류가 4가지이고 **종류마다 응답 스키마가 완전히 다른** 것이 핵심 난점이었습니다.

**어댑터를 통한 뷰모델 정규화 + 의미 없는 이력 필터링** — 종류별 상세가 서로 다른 키에 다른 형태로 담겨 옵니다
(어떤 종류는 `~Before`/`~After` 접미사 필드, 어떤 종류는 중첩 객체).
공통 `before`/`after` 뷰모델로 변환해 렌더링 컴포넌트는 단일 형태만 알면 되게 했고,
변환 단계에서 값 변화가 없는 수정 이벤트를 걸러 타임라인 노이즈를 제거했습니다.

```js
const normalizeHistory = (rows) =>
  rows.reduce((acc, row) => {
    const base = { ...row, updatedAt: formatDateTime(row.updatedAt) };

    if (row.target === 'TYPE_A') {
      // 중첩 객체형: before/after가 이미 분리되어 옴
      const detail = row.typeADetail;
      return [...acc, { ...base, before: detail.before, after: detail.after }];
    }

    if (row.target === 'TYPE_B') {
      // 접미사형: 한 객체 안에 ~Before / ~After가 나란히 들어옴
      const d = row.typeBDetail;
      const before = { status: d.statusBefore, dueDate: d.dueDateBefore, memo: d.memoBefore };
      const after  = { status: d.statusAfter,  dueDate: d.dueDateAfter,  memo: d.memoAfter  };

      // 값이 하나도 안 바뀐 MODIFY는 보여줄 가치가 없음 → 제외
      const unchanged = Object.keys(before).every((k) => before[k] === after[k]);
      if (row.type === 'MODIFY' && unchanged) return acc;

      return [...acc, { ...base, before, after }];
    }

    return [...acc, { ...base, ...row.defaultDetail }];
  }, []);
```

**무한 스크롤과 캐시 수명 관리** — 페이지 크기 미만 응답을 종료 조건으로 두고, 스크롤 끝 이벤트에 `debounce`를 걸었습니다.
패널을 닫을 때 캐시를 제거해, 다른 대상으로 재진입했을 때 **이전 대상의 이력이 잠깐 보이는 현상**을 없앴습니다.

```js
const { data, fetchNextPage } = useInfiniteQuery({
  queryKey: ['history', resourceId],
  queryFn: ({ pageParam }) => fetchHistory({ resourceId, page: pageParam, size: PAGE_SIZE }),
  initialPageParam: 0,
  getNextPageParam: (lastPage, _, lastParam) =>
    (lastPage?.list?.length ?? 0) < PAGE_SIZE ? undefined : lastParam + 1,
  enabled: visible,
});

const handleScrollEnd = debounce(fetchNextPage, 100);

const handleClose = () => {
  setVisible(false);
  queryClient.removeQueries({ queryKey: ['history'] }); // 다음 대상에 잔상이 남지 않도록
};
```

---

#### 깜빡임 없이 목록이 갱신되도록 필터를 유지한 채 다시 받아왔습니다.

**2024.07 – 2025.10**

여러 단계의 승인을 거치는 업무 워크플로 화면군(요청 / 처리 대기 / 처리 완료 목록)을 담당했습니다.

**문제** — 항목을 처리하면 목록이 즉시 갱신되어야 하는데, `invalidateQueries`로 전체를 무효화하면
로딩 스피너가 뜨고 화면이 깜빡였습니다. 사용자가 처리 직후 목록의 필터·스크롤 맥락을 잃는 것이 특히 문제였습니다.

**해결** — 현재 마운트된 목록의 queryKey에서 **적용 중인 필터를 역추출**해 같은 조건으로 백그라운드 재조회한 뒤,
결과를 캐시에 직접 주입합니다. 로딩 상태를 거치지 않으므로 목록이 그 자리에서 조용히 갱신됩니다.

```js
const FETCHER_BY_VARIANT = {
  REQUESTED:  fetchRequestedList,
  TO_PROCESS: fetchToProcessList,
  PROCESSED:  fetchProcessedList,
};

const refreshListInPlace = async (queryClient) => {
  // 지금 화면에 떠 있는 목록 쿼리를 찾는다
  const [entry] = queryClient.getQueriesData({ queryKey: ['list'], stale: true });
  if (!entry) return;

  // queryKey 자체가 필터를 담고 있으므로 여기서 되꺼낸다
  const [, variant, filter] = entry[0];
  const fetchList = FETCHER_BY_VARIANT[variant];
  if (!fetchList) return;

  // 같은 조건으로 다시 받아 캐시에 직접 주입 → invalidate 없이 무깜빡임 갱신
  const result = await fetchList(filter)();
  queryClient.setQueryData(['list', variant, filter], result);
};
```

같은 패턴이 필요한 5개 목록 도메인에 동일한 형태로 적용해, "액션 후 목록 갱신"을 팀 내 일관된 방식으로 정착시켰습니다.

---

#### 단일·다중 수정 폼을 하나로 합치고 체크한 필드만 검증하도록 했습니다.

**2025.04 · 2025.11 · 2026.07**

여러 항목을 선택해 한 번에 수정하는 화면군입니다. "체크한 필드만 전송한다"는 규약을 여러 도메인에서 일관되게 구현했습니다.

**단일/다중 폼의 통합 + 체크 상태와 검증의 결합 해소** — 별도 컴포넌트로 두면 필드 추가 시 양쪽에 중복 반영해야 합니다.
하나의 폼을 플래그로 분기하고, 체크 해제된 필드는 검증 대상에서 아예 제외했습니다.
(체크하지 않은 필드까지 검증이 돌아, 건드리지도 않은 항목 때문에 저장이 막히는 문제가 반복됐습니다.)

```jsx
<CheckFormItem
  check={!isSingle}            // 다중 수정일 때만 필드별 체크박스 노출
  required={isSingle}          // 단일에서만 필수, 다중은 선택 항목
  onCheckBoxChange={setIsFieldChecked}
>
  <FormItemLabel label={t('label.field')} required={isSingle} />
  <FormItemControl>
    <ValidatedItem value={value} onChange={handleChange}>
      <Dropdown options={options} />
    </ValidatedItem>
  </FormItemControl>
</CheckFormItem>
```

```js
// 체크된 필드만 검증 → 미선택 항목이 저장을 막지 않음
const validate = () =>
  Promise.all(
    FIELDS.filter((f) => checkedState[f.name])
          .map((f) => refs[f.name].current?.validate()),
  );

// 체크된 필드만 전송 → 나머지는 서버에서 기존 값 유지
const buildPayload = () =>
  FIELDS.reduce((payload, f) => {
    if (checkedState[f.name]) payload[f.name] = formValue[f.name];
    return payload;
  }, {});
```

**"변경 없음"과 "전부 삭제"의 구분** — 목록형 필드를 전부 비웠을 때 빈 배열을 보내면
서버가 미변경으로 처리해 삭제가 반영되지 않았습니다. 두 의도가 구분되도록 전송 규약을 정리했습니다.

```js
// keepExisting이 켜져 있으면 필드를 아예 생략 = "건드리지 않음"
// 꺼져 있고 목록이 비었으면 명시적 빈 값  = "전부 삭제"
const members = keepExisting
  ? undefined
  : (selectedMembers.length > 0 ? selectedMembers : null);
```

---

#### 설정 데이터를 파일로 주고받는 내보내기·불러오기를 구현했습니다.

**2025.09**

설정 데이터를 파일로 내보내 다른 환경으로 이전하는 기능입니다.

**파일 + 메타데이터 동시 전송** — 내보내기는 `blob` 응답으로 받고,
불러오기는 `FormData`에 파일 파트와 **JSON을 `Blob`으로 감싼 파트**를 분리해 담아 서버의 multipart 규격에 맞췄습니다.
(JSON을 문자열로 append하면 `text/plain`으로 전송되어 서버가 객체로 파싱하지 못합니다.)

```js
export const exportConfig = ({ id, fileName }) =>
  axios({
    url: `/api/config/export?id=${id}&fileName=${fileName}`,
    method: 'GET',
    responseType: 'blob',           // 바이너리 응답 → 다운로드로 연결
  }).then((res) => ({ data: res.data }));

export const importConfig = ({ file, input }) => {
  const form = new FormData();
  form.append('file', file[0].object);

  // content-type을 명시해 JSON 파트로 인식시킴
  form.append('input', new Blob([JSON.stringify(input)], { type: 'application/json' }));

  return axios.post('/api/config/import', form).then((res) => ({ data: res.data }));
};
```

**여러 건의 검증 실패 사유 전달** — 서버가 실패 사유를 배열로 주는데 단일 토스트로는 전달이 안 됐습니다.
오류 코드를 분기해 사유들을 개행 결합한 서브 메시지로 노출, 사용자가 무엇을 고쳐야 하는지 알 수 있게 했습니다.

```js
onError: (error) => {
  const { code, messages } = error?.response?.data ?? {};

  if (VALIDATION_ERROR_CODES.includes(code)) {
    showToast('error', t('msg.import.fail'), messages?.join('\n')); // 사유 전부 노출
  } else {
    showToast('error', t('msg.import.fail'));
  }
},
```

확장자 화이트리스트·필수 파일 검증은 재사용 가능한 룰 함수로 분리해 다른 업로드 폼에서도 쓰도록 했습니다.

```js
const rules = [
  validateFileRequired(t('msg.file.required')),
  validateAllowedExtension(['.json'], t('msg.file.extension')),
];
```

#### 흩어져 있던 대시보드 위젯을 공통 컴포넌트로 정리했습니다.

**2024.11 – 2025.01**

- 여러 종류의 순위·현황 위젯을 공통 테이블 컴포넌트 단위로 분리하고 empty placeholder를 규격화했습니다.
- 전역 필터를 각 위젯 쿼리에 반영하고, 중복 호출되던 조회 로직을 제거했습니다.
- **요약에서 상세로의 연결** — 차트 범례를 클릭하면 해당 조건이 필터로 적용된 상세 목록으로 이동하도록
  필터 프리셋 전달 경로를 만들어, 탐색 흐름을 끊기지 않게 했습니다.

---

#### 수만 개 노드의 트리를 펼치는 만큼만 받아오도록 바꿨습니다.

**2023.11**

파일·디렉터리 구조를 탐색기 형태로 보여주는 트리 뷰입니다.
노드가 수만 개에 달해 **전체 트리를 한 번에 받으면 응답에 수 초가 걸리고 메모리도 감당이 안 되는** 상황이었습니다.
GraphQL 조회 계층을 설계해 "필요한 깊이만 그때그때 받는" 구조로 바꿨습니다.

**루트 조회와 하위 조회의 분리** — 진입 시에는 최상위 노드만 받고, 사용자가 노드를 펼칠 때
해당 노드의 하위를 별도 쿼리로 가져옵니다. 초기 로딩 비용이 트리 전체 크기와 무관해집니다.

```js
// ① 진입 시: 최상위 노드만
export const fetchTreeRootNodes = (scopeKey) => async () => {
  const { result } = await requestGQL(gql`
    query fetchRootNodes($scopeKey: String!) {
      result: fetchRootNodes(scopeKey: $scopeKey) {
        id  name  parentId
        isParent          # 펼침 가능 여부를 서버가 판정해 내려줌
        hasFlag           # 하위에 주목할 항목이 있는지 (뱃지 표시용)
        children { id name parentId isParent }  # 한 단계 미리 받아 첫 펼침을 즉시 처리
      }
    }`, { scopeKey });
  return result;
};

// ② 노드를 펼칠 때: 그 노드의 하위만
export const fetchSubTree = async ({ scopeKey, parentId }) => {
  const { result } = await requestGQL(gql`
    query fetchSubTree($scopeKey: String!, $parentId: Long!) {
      result: fetchSubTree(scopeKey: $scopeKey, parentId: $parentId) {
        id  name  parentId  isParent
        children { id name parentId isParent isLeaf }
      }
    }`, { scopeKey, parentId });
  return result;
};
```

**확장 가능 여부를 서버가 판정** — 클라이언트가 "이 노드에 자식이 있나"를 알려면
원래는 자식을 미리 조회해봐야 합니다. 응답 스키마에 `isParent` / `isLeaf` 플래그를 포함시켜,
**펼침 화살표를 그리기 위한 목적만의 조회를 없앴습니다.**

**한 단계 선행 로딩** — 각 조회가 노드와 그 자식까지 1-depth를 함께 반환하도록 설계했습니다.
이미 받아둔 자식을 펼칠 때는 네트워크 왕복 없이 즉시 열리고, 더 깊이 들어갈 때만 요청이 나갑니다.
"매번 요청"과 "전부 미리 받기" 사이의 절충점입니다.

**검색은 트리가 아닌 평면 목록으로** — 검색 결과를 트리 형태로 복원하려면 매칭된 모든 노드의
조상 경로를 재구성해야 해 비용이 큽니다. 검색은 페이지네이션이 붙은 **평면 목록**으로 응답받아
무한 스크롤로 렌더링하고, 사용자가 항목을 선택한 시점에만 조상 경로를 조회해
트리에서 그 위치를 펼쳐 보여주는 방식으로 나눴습니다.

```js
// 검색: 트리 구조 없이 평면 목록 + 페이지네이션
export const searchNodes = async ({ scopeKey, keyword, pageNum, pageSize }) => {
  const { result } = await requestGQL(gql`
    query searchNodes($scopeKey: String!, $keyword: String!, $filter: NodeFilter!) {
      result: searchNodes(scopeKey: $scopeKey, keyword: $keyword, filter: $filter) {
        list { id name type isParent hasFlag }
        page  size  total  filtered
      }
    }`, {
    scopeKey,
    keyword,
    filter: { page: { page: pageNum, size: pageSize }, sorts: [{ key: 'id', order: 'asc' }] },
  });
  return result;
};

// 선택한 항목에 한해 조상 경로를 받아 트리를 그 지점까지 펼침
const revealInTree = async (nodeId) => {
  const path = await fetchAncestorPath({ scopeKey, childId: nodeId });
  setExpandedKeys((prev) => [...new Set([...prev, ...path])]);
};
```

담당 범위는 트리 조회 계층 설계와 지연 로딩 연동이며, 트리 렌더링 컴포넌트는 팀원과 협업했습니다.

---

#### 마이그레이션 대응 훅과 상태 보존형 mock으로 개발 환경을 정비했습니다.

**라이브러리 메이저 마이그레이션 대응 — `useQueryEffects`**

TanStack Query v5에서 제거된 `onSuccess`/`onError`/`onSettled` 콜백을 대체하는 훅을 직접 구현했습니다.
단순히 `useEffect`로 옮기면 **리렌더마다 콜백이 재실행되는** 문제가 있어,
직전 상태를 기억해 성공/실패로 **전이하는 순간에만 1회** 실행되도록 했습니다.
제네릭 타입을 유지해 호출부의 데이터 타입 추론이 끊기지 않습니다.

```ts
export function useQueryEffects<TData, TError>(
  query: UseQueryResult<TData, TError>,
  { onSuccess, onError }: QueryEffectsOptions<TData, TError>,
) {
  const prevRef = useRef({ isSuccess: false, isError: false });

  useEffect(() => {
    const { isSuccess, isError, data, error } = query;
    const prev = prevRef.current;

    // 이미 성공 상태였다면 재실행하지 않음 → "전이" 시점에만 1회
    if (isSuccess && onSuccess && !prev.isSuccess) onSuccess(data as TData);
    if (isError && onError && !prev.isError) onError(error as TError);

    prevRef.current = { isSuccess, isError };
  }, [query.isSuccess, query.isError, query.data, query.error, onSuccess, onError]);

  return query;
}
```

**API 선행 개발 워크플로 (MSW)**

백엔드 API 완성 전에 화면을 먼저 개발하기 위해 MSW 핸들러를 20여 개 작성했습니다.
정적 fixture에 그치지 않고 **조회 → 수정 → 재조회가 실제로 반영되는 상태 보존형 mock store**를 만들어,
연동 전에 폼 왕복 시나리오까지 검증할 수 있게 했습니다.

```js
// 모듈 스코프에 상태를 두어 요청 간 변경이 유지됨
let store = { enabled: false, criteria: [] };

export const handlers = [
  graphql.query('getConfig', () => HttpResponse.json({ data: { result: store } })),

  graphql.mutation('updateConfig', ({ variables }) => {
    store = { ...store, ...variables.input };      // 수정이 실제로 반영
    return HttpResponse.json({ data: { result: store } });
  }),
];
```

환경 변수로 토글하고, 비활성 시 서비스 워커 등록을 해제해 운영 빌드에 영향이 없도록 했습니다.

```tsx
useEffect(() => {
  if (mockingEnabled) {
    import('./mocks/browser').then(({ worker }) =>
      worker.start({ onUnhandledRequest: 'bypass' }),
    );
  } else {
    // 이전에 등록된 워커가 남아 운영 요청을 가로채지 않도록 정리
    navigator.serviceWorker?.getRegistrations()
      .then((regs) => regs.forEach((r) => r.unregister()));
  }
}, []);
```

**국제화(i18n)**

한/영 리소스를 지속 관리하며 하드코딩된 문자열을 리소스로 이관하고,
**리소스 키를 그대로 화면에 노출하는 전용 로케일**을 활용해 누락 키를 시각적으로 탐지했습니다.
영문에서만 발생하는 레이아웃 파손(컬럼 너비·줄바꿈·말줄임 잘림)을 로케일 기반 스타일 분기로 다수 해결했습니다.

```js
const i18n = {
  defaultLocale: 'ko',
  locales: ['ko', 'en', 'key'],  // 'key' = 번역 대신 키를 그대로 렌더 → 누락/하드코딩 탐지
};
```

---

### E2E 테스트 구축 및 자동화

**2025.08 – 2026.01**

Playwright 기반 E2E 테스트 자동화 파이프라인 구축

- `Playwright`로 **모든 서비스 사용 시나리오의 E2E 테스트 자동화 파이프라인**을 구축했습니다.
  - **220개의 테스트 코드**를 작성 및 유지보수하며, **수동 전수 테스트 시간을 75% 단축**했습니다.
- `Docker` + `gitlab-runner`로 **테스트 CI/CD 환경을 구축**하고, 테스트를 제품 관리 프로세스에 정착시켰습니다.
- **MCP(Model Context Protocol) 서버를 직접 설계 및 운영**했습니다.
  - 제품의 기능 명세, 코드 컨벤션, 테스트 시나리오를 컨텍스트로 주입하여, **각 기능 담당자가 AI의 도움을 받아 테스트 코드를 직접 작성하고 디버깅**할 수 있는 환경을 만들었습니다.
- 테스트 작성 가이드를 문서화해, 테스트가 특정 담당자에게 몰리지 않도록 팀 전체로 확산시켰습니다.
- `Playwright` `Docker` `gitlab-runner` `React` `Cursor Agent`

---

### Emotion → Tailwind 마이그레이션

**2026.02 – 2026.04** 

- **105개 컴포넌트의 UI 영향도가 없어야 한다**는 제약 아래 진행했습니다.
- **Claude의 서브 에이전트(Sub-agent) 구조를 활용한 Agent Teams 방식**을 실무에 적용했습니다.
  - '컨벤션 분석 에이전트'가 기존 코드 패턴을 파악하고, 그 결과를 '변환 에이전트'에 전달해 1차 마이그레이션을 수행하는 파이프라인을 구성했습니다.
  - 또한 '테스트 담당 에이전트'가 마이그레이션 후 스냅샷을 기존 스냅샷과 비교하는 테스트를 수행하도록 했습니다.
- `BackstopJS` 시각 회귀 테스트를 CI에 연동해 **500여 개의 Storybook story를 자동 검증**하며, UI 변경 없이 안전하게 전환했습니다.
- 마이그레이션 과정과 의사결정을 문서화해 팀에 공유했습니다. (**[관련 글](https://velog.io/@josuncom)**)
- `TypeScript` `React` `TailwindCSS` `Emotion.js` `Nx` `Yarn Workspaces` `Rollup` `Storybook` `BackstopJS`

#### className이 비대해진다는 tailwind의 한계를 해결했습니다.

css-in-js 스타일의 문자열 조립을 컴포넌트에서 걷어내고, **변형(variant)은 `cva`로 선언**하고 **실제 스타일 값은 CSS 파일**로 옮겼습니다. 

```diff
- import styled from '@emotion/styled';
- import { css, useTheme } from '@emotion/react';
+ import { cva, type VariantProps } from 'class-variance-authority';
+ import { cn } from './cn';

  export interface BannerProps {
    className?: string;
    type?: 'success' | 'error' | 'info' | 'pending';
-   /** Support for ThemeProvider, do not set this attribute directly */
-   theme?: Theme;
  }

- const bannerStyle = (props: StyledBannerProps) => css`
-   display: flex;
-   width: 100%;
-   background: ${props.theme.colors.banner.background};
-   border: 1px solid ${props.theme.colors.banner.border};
-
-   .ds-banner-accent {
-     width: 8px;
-     background: ${props.theme.colors.banner.accent[props.type]};
-   }
- `;
- const StyledBanner = styled.div`${(props) => bannerStyle(props)}`;
+ /* 변형 선언만 남기고, 실제 스타일 값은 Banner.css로 이동 */
+ const bannerVariants = cva('ds-banner', {
+   variants: {
+     type: {
+       success: '',
+       error: '',
+       info: '',
+       pending: 'ds-banner--pending', // 애니메이션 등 타입 고유 규칙만 클래스로
+     },
+   },
+   defaultVariants: { type: 'info' },
+ });

  export function Banner({ className, type = 'info', children }: BannerProps) {
-   const theme = useTheme();
    return (
-     <StyledBanner theme={theme} type={type} className={className}>
+     // data-*로 타입을 넘기면 CSS 쪽에서 accent 토큰만 스위칭하면 된다
+     <div className={cn(bannerVariants({ type }), className)} data-banner-type={type}>
        <div className="ds-banner-accent" />
        {children}
-     </StyledBanner>
+     </div>
    );
  }
```


- `VariantProps<typeof bannerVariants>`로 **variant 정의가 곧 타입**이 되게 해서 스타일 변수와 도메인을 어긋날 수 없도록 했습니다.
- 타입별 분기를  `data-` 속성 + CSS 변수 스위칭으로 처리해 **tailwind의 단점인 className의 비대함을 해결**했습니다.
- `tailwind-merge`로 스타일 충돌을 처리했습니다.

```diff
+ import { type ClassValue, clsx } from 'clsx';
+ import { twMerge } from 'tailwind-merge';
+
+ /**
+  * clsx로 조건부 클래스를 모으고, tailwind-merge로 충돌하는 유틸을 뒤쪽 우선으로 병합한다.
+  */
+ export function cn(...inputs: ClassValue[]): string {
+   return twMerge(clsx(inputs));
+ }
```

#### 테스트 정합성을 위해 테스트를 포함한 파이프라인을 구축했습니다.

Storybook을 Docker로 띄우고 UI 회귀 테스트를 실행합니다.
위 과정을 Gitlab Pipeline으로 구축해, 마이그레이션 커밋을 트리거로 테스트까지 수행합니다.

```diff
  variables:
+   # nx affected가 base 커밋을 diff 할 수 있도록 히스토리 확보 (shallow clone 방지)
+   GIT_DEPTH: '100'
+   # CI에서 nx 데몬 비활성 (러너 간 상태 공유/좀비 프로세스 방지)
+   NX_DAEMON: 'false'

+ open_server:
+   script:
+     - docker run -d -p 6006:6006 --name storybook_ci storybook-image
+     - |
+       for i in $(seq 1 60); do
+         if curl -fsS "$STORYBOOK_ORIGIN/iframe.html" > /dev/null; then
+           echo "Storybook is ready."; exit 0
+         fi
+         echo "Waiting for Storybook... ($i/60)"; sleep 2
+       done
+       docker logs storybook_ci || true; exit 1

  test:
    script:
-     - yarn playwright install --with-deps chromium
+     # --with-deps는 apt-get(Debian/Ubuntu) 전용. AlmaLinux 러너에서는 바이너리만 설치
+     - yarn playwright install chromium
+     - |
+       BASE="${CI_COMMIT_BEFORE_SHA}"
+       # 브랜치 첫 파이프라인이면 0 SHA → master로 폴백
+       if [ -z "$BASE" ] || [ "$BASE" = "0000000000000000000000000000000000000000" ]; then
+         git fetch origin master --depth=100 || true
+         BASE="origin/master"
+       fi
+       node scripts/visual-affected.mjs --base="$BASE"
```


기준 스냅샷 자체도 **로컬 macOS/Windows와 CI(Linux)의 폰트 힌팅·서브픽셀 차이** 때문에 false positive가 나서, CI 러너에서 생성한 것으로 교체했습니다.


#### 테스트가 필요한 컴포넌트만 테스트하도록 최적화했습니다.

105개 패키지의 스타일 엔진을 통째로 바꾸면서 **"UI가 안 바뀌었다"를 증명**하는 것 중요했습니다.
하지만 500개가 넘는 전체 스토리를 매 커밋마다 캡처하는 건 효율적이지 못한 방법입니다.
**"어떤 컴포넌트를 수정하면 그걸 import 하는 부모 컴포넌트에 영향이 간다"** 는 요구를 Nx 프로젝트 그래프로 풀었습니다.

```diff
+ // scripts/visual-affected.mjs
+ //   node scripts/visual-affected.mjs                 # master 대비 변경 패키지 + 부모들
+ //   node scripts/visual-affected.mjs --with-css      # CSS 셀렉터로만 결합된 패키지도 포함
+ //   node scripts/visual-affected.mjs --reference     # baseline 스냅샷 생성
+
+ /** 변경분 → affected 프로젝트(= 변경 패키지 + 이를 의존하는 부모 전체) */
+ function affectedProjects(nameToRoot) {
+   if (projectsOpt) {
+     // 명시 모드: 지정 패키지를 "변경됨"으로 보고 역방향 의존까지 확장
+     const files = projectsOpt.split(',')
+       .map((n) => nameToRoot.get(n.trim()))
+       .filter(Boolean)
+       .map((r) => `${r}/package.json`);
+     return JSON.parse(nx(['show', 'projects', '--affected', `--files=${files}`, '--json']));
+   }
+   return JSON.parse(nx(['show', 'projects', '--affected', `--base=${base}`, '--json']));
+ }
```

**import 그래프로 잡히지 않는 두 가지 결합**을 별도로 처리한 것이 실무적으로 중요했습니다.

```diff
+ // (1) CSS 결합: JS import 없이 .ds-<comp> 셀렉터로만 엮인 패키지
+ function cssCoupledRoots(affectedRoots, nameToRoot) {
+   const extra = new Set();
+   const allRoots = [...nameToRoot.values()];
+   for (const root of affectedRoots) {
+     const comp = root.split('/').pop();            // packages/atoms/badge → badge
+     const res = spawnSync('git', ['grep', '-l', '--', `ds-${comp}`, 'packages/'],
+                           { cwd: ROOT, encoding: 'utf8' });
+     if (res.status !== 0 || !res.stdout) continue;
+     for (const f of res.stdout.split('\n').filter(Boolean)) {
+       // 가장 깊게 매칭되는 root = 그 파일의 소유 패키지
+       const owner = allRoots
+         .filter((r) => f === r || f.startsWith(r + '/'))
+         .sort((a, b) => b.length - a.length)[0];
+       if (owner) extra.add(owner);
+     }
+   }
+   return [...extra];
+ }
+
+ // (2) 전역 토큰 변경: 그래프 밖에서 모든 컴포넌트에 영향 → 전체 회귀를 경고로 안내
+ if (projects.includes('@ds/styles')) {
+   console.warn('⚠️  전역 토큰 변경 감지 — 전체 회귀 권장');
+ }
+
+ // 대상 0개면 조용히 통과 (파이프라인을 막지 않는다)
+ if (roots.length === 0) {
+   console.log('변경 영향권에 story 패키지가 없음 → 시각 회귀 스킵.');
+   process.exit(0);
+ }
```


UI 회귀 검증은 수동 QA에서 CI 자동 시각 회귀로 바뀌었습니다.
본 마이그레이션의 병목은 마이그레이션 자체가 아니라 **영향도 파악과 검증**이었고, 인프라를 먼저 만든 판단으로 전체 기간을 줄였습니다.

---

### app router로의 마이그레이션

**2026.02 – 2026.06**

- 운영 중인 제품이라 **점진적 마이그레이션**을 택했습니다. 전환한 페이지만 `app/`으로 옮기고 나머지는 `pages/`에 그대로 둔 채 **같은 서버·같은 세션에서 함께 동작**시켜, 배포 단위를 쪼개고 롤백 범위를 페이지 단위로 한정했습니다.
- **RSC와 클라이언트 컴포넌트의 경계를 설계**하고 서버 prefetch 구조를 도입해 **초기 로딩 속도(FCP)를 60% 개선**했습니다.
- 기존 `React-Query` 기반 데이터 페칭 계층을 서버 컴포넌트 환경에 맞게 재설계했습니다.
- 점진적 전환이 가능하도록 순서와 기준을 정의하고 문서로 남겨, 팀원들이 동일한 방식으로 작업할 수 있게 했습니다.
- `TypeScript` `Next` `React` `RSC` `React-Query` `GraphQL` `Emotion.js`

#### 데이터 취득 실패가 페이지 전체 실패로 이어지던 구조를 React Server Component로 옮겼습니다.

기존에는 인증·프리패치·에러 처리가 `getServerSideProps` 한 함수에 모여 있고, 전체 캐시를 JSON으로 왕복시켜 props로 넘기는 구조였습니다.
**데이터 취득 실패가 곧 페이지 전체 실패**가 되는 구조여서, RSC로 옮기면서 실패를 삼키고 클라이언트가 이어받도록 바꿨습니다.

```diff
- // pages/board/index.js  (Pages Router)
- export default function Board() {
-   useAuthSession();                       // 클라이언트 훅이 페이지 최상단에 결합
-   return (
-     <PageLayout current="board">
-       <Head><title>{formatPageTitle(t('label.board'))}</title></Head>
-       <Articles />
-     </PageLayout>
-   );
- }
-
- export async function getServerSideProps(context) {
-   const { token, queryClient } = await initializeSession(context);
-   if (!token) {
-     return { redirect: { destination: `/?redirect=${context.resolvedUrl}`, permanent: false } };
-   }
-   try {
-     await initializePage({ queryClient, page: 'board', context });
-     return {
-       props: {
-         // 전체 캐시를 JSON 왕복시켜 props로 전달
-         dehydratedState: JSON.parse(JSON.stringify(dehydrate(queryClient))),
-         locale: context.locale,
-       },
-     };
-   } catch (error) {
-     return handleSsrError(error, context.locale);   // 실패 = 페이지 전체 실패
-   }
- }

+ // app/board/page.tsx  (App Router / RSC)
+ export default async function BoardPage() {
+   const queryClient = new QueryClient();
+
+   try {
+     const [session, restriction] = await Promise.all([
+       getSessionServer(),                 // 서버 전용 GraphQL 호출
+       getRestrictionServer(),
+     ]);
+     queryClient.setQueryData(['session'], session.user);
+     queryClient.setQueryData(['authorities'], session.authorities);
+     queryClient.setQueryData(['restriction'], restriction);
+   } catch {
+     // 서버 prefetch 실패는 삼키고, 아래 DataProvider가 클라이언트에서 폴백 로딩
+   }
+
+   return (
+     <HydrationBoundary state={dehydrate(queryClient)}>
+       <AuthSessionClient />               {/* 클라이언트 전용 관심사 격리 */}
+       <BoardDataProvider>
+         <PageLayoutShell current="board">
+           <Articles />
+         </PageLayoutShell>
+       </BoardDataProvider>
+     </HydrationBoundary>
+   );
+ }
+
+ // <title>은 metadata 객체로 이동 → 클라이언트 JS 없이 처리
+ export const metadata: Metadata = { title: 'Board' };
```

#### 데이터 페칭은 서버, 상호작용은 클라이언트로 컴포넌트 경계를 설계했습니다.

기존 컴포넌트 대부분이 Emotion·전역 스토어·이벤트 핸들러에 의존하고 있었습니다.
**"데이터 페칭은 서버, 상호작용은 클라이언트"** 로 경계를 설정했습니다.

```
app/dashboard/page.tsx          (RSC)   — GraphQL 병렬 fetch → QueryClient seed → dehydrate
  └ HydrationBoundary                   — 직렬화된 캐시를 클라이언트로 전달
      └ DashboardDataProvider  ('use client') — 서버 데이터 유무 확인 + 실패 시 폴백 로딩
          └ DashboardClient    ('use client') — 상호작용 / 렌더링
```

**경계의 매개체를 React Query 캐시로 통일** — 서버가 주입한 데이터를 클라이언트가 **같은 queryKey로 그대로 받게** 해서,
기존 컴포넌트의 데이터 접근 코드를 거의 수정하지 않고 SSR 이득만 얻었습니다.

```diff
  // [서버] app/dashboard/page.tsx
+ // 클라이언트 첫 렌더의 queryKey와 정확히 일치시키는 것이 관건.
+ // 하나라도 어긋나면 캐시 미스 → 클라이언트가 전부 다시 요청한다.
+ const bodyFilter = undefined;
+ const bodyPage = { size: 10, page: 0 };
+
+ queryClient.setQueryData(['summaryCounts', 'dashboardInfo', bodyFilter], summary);
+ queryClient.setQueryData(['vulnerableItems', bodyFilter, bodyPage], items);

  // [클라이언트] DashboardClient.tsx — 호출부는 변경 없음
  const { data } = useQuery({
    queryKey: ['summaryCounts', 'dashboardInfo', filter],
    queryFn: fetchSummaryCounts(filter),
+   staleTime: DASHBOARD_STALE_TIME,
  });
```

**폴백 계층 분리** — 서버 prefetch는 인증 만료·백엔드 장애로 실패할 수 있습니다. 클라이언트 Provider가 **부족한 데이터만** 재요청해 화이트스크린을 막습니다.

```diff
+ // app/dashboard/DashboardDataProvider.tsx
+ 'use client';
+ export default function DashboardDataProvider({ children }) {
+   const queryClient = useQueryClient();
+
+   // 서버가 이미 채워준 경우 첫 렌더부터 준비 완료 → 추가 요청·깜빡임 없음
+   const [isDataReady, setIsDataReady] = useState(
+     () => !!(queryClient.getQueryData(['authorities']) &&
+              queryClient.getQueryData(['projects'])),
+   );
+
+   useEffect(() => {
+     if (isDataReady) return;
+     (async () => {
+       try {
+         // 없는 것만 선택적으로 채운다 (전량 재요청 아님)
+         if (!queryClient.getQueryData(['authorities'])) await fetchSession(queryClient);
+         if (!queryClient.getQueryData(['projects'])) {
+           queryClient.setQueryData(['projects'], await fetchProjects());
+         }
+       } catch {
+         // 폴백까지 실패해도 화면이 영구히 빈 상태로 멈추지 않도록 통과시킨다.
+       }
+       setIsDataReady(true);
+     })();
+   }, [queryClient, isDataReady]);
+
+   if (!isDataReady) return null;
+   return <>{children}</>;
+ }
```

**클라이언트 전용 관심사 격리** — 세션 폴링 훅 하나 때문에 페이지 전체가 클라이언트가 되는 것을 막고, 경계를 트리 아래쪽으로 밀어냈습니다.

```diff
+ // app/board/AuthSessionClient.tsx
+ // return null인 얇은 클라이언트 컴포넌트로 분리 → 페이지 트리는 RSC 유지
+ 'use client';
+ export default function AuthSessionClient() {
+   useAuthSession();
+   return null;
+ }

  // components/layout/PageLayoutShell.tsx — 'use client' 없음 (서버 컴포넌트)
  export default function PageLayoutShell({ current, children }) {
    return (
-     <LayoutWrapper>          {/* 전체가 클라이언트 */}
-       <ProductHeader />
-       <MainArea current={current}>{children}</MainArea>
-     </LayoutWrapper>
+     <LayoutWrapperClient>    {/* 클라이언트 */}
+       <ProductHeader />      {/* 서버 컴포넌트 유지 */}
+       <MainAreaClient current={current}>{children}</MainAreaClient>
+     </LayoutWrapperClient>
    );
  }
```

#### RSC에서 호출할 수 없던 GraphQL 레이어에 서버 전용 요청 함수를 추가했습니다.

기존 requestGQL은 axios 기반에 브라우저 쿠키·인터셉터·window 의존이 섞여 있어 RSC에서 호출할 수 없었습니다.
클라이언트 코드를 수정하는 것보다 **서버 전용 request 함수를 추가**했습니다.

```diff
+ // utils/server/requestGQL.ts  — 신설 (RSC 전용)
+ import { cookies } from 'next/headers';
+
+ export async function requestGQLServer(query: string, variables?: Record<string, any>) {
+   // 브라우저가 없는 실행 컨텍스트이므로 세션 쿠키를 명시적으로 전달해
+   // 인증 컨텍스트를 서버 렌더 경로에서도 그대로 유지한다.
+   const cookieHeader = (await cookies())
+     .getAll()
+     .map((c) => `${c.name}=${c.value}`)
+     .join('; ');
+
+   const response = await fetch(`${INTERNAL_GATEWAY}/graphql`, {
+     method: 'POST',
+     headers: {
+       'Content-Type': 'application/json',
+       Cookie: cookieHeader,
+       'X-Internal-Call': 'true',
+     },
+     body: JSON.stringify({ query, variables }),
+     cache: 'no-store',       // 사용자별 인증 데이터이므로 캐시 금지
+   });
+
+   if (!response.ok) throw new Error(`GraphQL request failed: ${response.status}`);
+
+   const json = await response.json();
+   // GraphQL은 200 응답 안에 errors를 담으므로 명시적으로 throw
+   // → 상위 Promise.allSettled가 쿼리 단위로 격리
+   if (json.errors) throw new Error(`GraphQL errors: ${JSON.stringify(json.errors)}`);
+   return json.data;
+ }
```

응답 가공 로직을 클라이언트 버전과 **동일한 형태로 맞춰**, 서버 seed / 클라이언트 폴백 중 어느 경로로 채워져도 캐시에 들어가는 shape이 같도록 했습니다.

```diff
+ // utils/server/getSessionServer.ts
+ export async function getSessionServer() {
+   const { result } = await requestGQLServer(`{
+     result: selectCurrentUser {
+       user { id name email locale globalRoles { authorities { key } } }
+     }
+   }`);
+
+   // 클라이언트 버전과 동일한 평탄화 규칙을 적용해야
+   // 어느 경로로 채워져도 소비 코드가 동일하다.
+   const authorities = result.user.globalRoles
+     .flatMap((role) => role.authorities.map((a) => a.key))
+     .map((key) => `system-${key}`);
+
+   return { user: result.user, authorities };
+ }
```

#### 디렉터리 구조를 FSD(Feature-Sliced Design)로 재편했습니다.

`components/` 아래에 도메인이 평평하게 쌓여 있어, 대시보드 위젯이 게시판 유틸을 참조하는 식의 무규칙 의존이 있었습니다.

```diff
- components/
-   dashboard/DashboardHeader.js
-   dashboard/DashboardChart.js
-   board/Articles.js
- request/get/getDashboardProjects.js      # 대시보드에서만 쓰는데 공용 API에 위치

+ _pages/                                   # slice = 페이지 도메인
+   dashboard/
+     ui/     header/ summary/ info/        # 화면 조각
+     api/    getDashboardProjects.ts       # 이 도메인에서만 쓰는 API 호출
+     config/ dashboard-query.ts            # 도메인 상수(staleTime 등)
+     index.ts                              # public API — 외부는 여기로만 접근
+   board/
+     ui/ api/ index.ts
```

```diff
+ // _pages/dashboard/index.ts — public API
+ // 내부 파일 이동이 외부로 파급되지 않도록 노출 지점을 한 곳으로 고정
+ export { default as DashboardHeader } from './ui/header/DashboardHeader';
+ export { default as DashboardSummary } from './ui/summary/DashboardSummary';

  // 소비 측
- import DashboardHeader from '../../components/dashboard/DashboardHeader';
- import { getDashboardProjects } from 'request/get/getDashboardProjects';
+ import { DashboardHeader, DashboardSummary } from '_pages/dashboard';
+ import { getDashboardProjects } from '_pages/dashboard/api/getDashboardProjects';
```

디렉터리명은 `widgets` 대신 `_pages`로 정했습니다. 언더스코어가 붙으면 App Router가 라우트로 해석하지 않는 private folder가 되므로,
라우팅 규칙과 충돌하지 않으면서 FSD의 pages 레이어 의미를 유지할 수 있습니다.

#### 3단 요청 워터폴과 hydration 직후의 중복 요청을 제거했습니다.

**요청 워터폴 제거** — 대시보드는 초기 데이터 9개 + 본문 데이터 6개가 필요했는데, 클라이언트에서 순차·조건부로 호출해 3단 워터폴이 생겼습니다.

```diff
- // [Before] 클라이언트에서 순차·조건부 → 워터폴
- const { data: session } = useQuery(...);                       // 1단계
- const { data: projects } = useQuery({ enabled: !!session });   // 2단계 (세션 대기)
- const hasData = projects?.length > 0;
- const { data: summary } = useQuery({ enabled: hasData });      // 3단계 (판정 대기)

+ // [After] 서버에서 단일 배치로 병렬 발사
+ // 본문 데이터는 원래 "표시할 데이터가 있는가" 판정 이후에만 필요하지만,
+ // 판정을 기다리면 워터폴이 생기므로 투기적으로 함께 요청하고
+ // HTML에 직렬화할지만 조건부로 결정한다.
+ const [session, projects, summary, charts, items, recent, /* … */] =
+   await Promise.allSettled([
+     getSessionServer(),
+     getProjectsServer(),
+     getSummaryServer(bodyFilter),
+     getChartsServer(bodyFilter),
+     getVulnerableItemsServer(bodyFilter, bodyPage),
+     /* … 총 15개 */
+   ]);
+
+ // allSettled이므로 한 쿼리 실패가 페이지 전체를 무너뜨리지 않는다
+ if (session.status === 'fulfilled') {
+   queryClient.setQueryData(['session'], session.value.user);
+ }
```

**hydration 직후 중복 요청 차단** — 전역 `staleTime`이 0이라 서버가 채워준 데이터도 mount 즉시 stale로 판정돼 `refetchOnMount`가 걸렸습니다.
**서버가 이미 가져온 데이터를 클라이언트가 곧바로 다시 받아오는 이중 왕복**이 발생하던 지점입니다.

```diff
+ // _pages/dashboard/config/dashboard-query.ts
+ // 프리패치 대상 쿼리에만 부여해 hydration 직후 중복 요청을 막는다.
+ export const DASHBOARD_STALE_TIME = 60 * 1000;

  // app/client-providers.tsx
- // page.tsx에도 HydrationBoundary가 있어 dehydrated state가 이중 직렬화되고 있었음
- <HydrationBoundary state={dehydrate(queryClient)}>{children}</HydrationBoundary>
+ {children}

- // 매 렌더마다 새 객체 → 하위 트리 전체 리렌더 유발
- const dehydratedState = dehydrate(queryClient);
+ const dehydratedState = useMemo(() => dehydrate(queryClient), [queryClient]);
```
---

### Atomic design 디자인시스템 라이브러리 개발

> Sparrow Enterprise를 포함해 사내 여러 제품이 공유하는 컴포넌트 라이브러리입니다.
> 하나의 컴포넌트가 여러 제품·화면에서 재사용되므로, 개별 화면 로직보다
> **범용적인 상태 모델링과 API 설계**, 그리고 **회귀 없는 변경**이 핵심 과제였습니다.

`React` `TypeScript` `Emotion` `Nx` `Yarn Workspaces` `Rollup` `Storybook` `react-dnd` `Changesets` `BackstopJS` `Playwright` `Docker`


#### 재사용 가능한 "동적 리스트" 아키텍처를 설계했습니다.

**2025.07 – 2025.08 (최초 개발), 2026.04 (조치 우선순위 컴포넌트로 재사용)**

- 이슈 검출 규칙의 예시 코드 입력처럼, 항목을 자유롭게 추가·삭제·순서 변경하면서 전체를 한 번에 검증해야 하는 요구사항이 여러 화면에서 반복적으로 등장
  - 각 행은 자신의 값만 알고 있는데, 상위에서 "전체 제출"을 누르면 모든 행이 동시에 검증되어야 하는 문제
  - 각 행이 `useImperativeHandle`로 `validate()`를 노출하고, 리스트 컴포넌트가 ref 배열로 이를 수집해 `Promise.all`로 한 번에 실행하는 imperative 검증 API를 설계하여 해결
- `react-dnd`로 행을 드래그해 순서를 바꿀 때, 배열 값뿐 아니라 각 행이 들고 있는 ref도 같은 인덱스로 함께 재배치되지 않으면 검증 API가 엉뚱한 행을 가리키는 문제
  - `immutability-helper`의 `$splice`로 값 배열과 ref 배열을 항상 같은 트랜잭션에서 재정렬해 정합성을 유지하도록 처리
- 외부 `value`가 갱신될 때마다 매번 새 uuid를 부여하면 React key가 바뀌어 불필요한 리마운트(포커스 손실, 드래그 애니메이션 깨짐)가 발생하는 문제
  - 기존 인덱스에 대응하는 항목은 이전 id를 유지하고, 새로 추가된 항목에만 새 id를 부여하는 동기화 effect를 작성하여 해결
- Context 기반 compound component(`List.Footer`)로 "항목 추가" 트리거를 노출해, 사용하는 쪽에서 버튼 위치·문구를 자유롭게 커스터마이징할 수 있도록 API 설계
  - 같은 패턴을 이후 조치 우선순위 기준(`PriorityCriteria`) 컴포넌트에도 그대로 재사용하여 아키텍처의 범용성을 검증

```tsx
interface RowHandle {
  validate: () => Promise<void>;
}

const DynamicList = forwardRef((props: DynamicListProps, ref) => {
  const rowRefs = useRef<(RowHandle | null)[]>([]);
  const [items, setItems] = useState(
    props.value.map((v) => ({ ...v, key: generateId() })),
  );

  // 여러 행을 한 번에 검증하는 imperative API
  useImperativeHandle(ref, () => ({
    validate: () =>
      Promise.all(rowRefs.current.map((row) => row?.validate?.()))
        .then(() => Promise.resolve())
        .catch(() => Promise.reject()),
  }));

  // 드래그로 순서를 바꾸면 값 배열과 ref 배열을 같은 트랜잭션에서 재정렬
  const handleReorder = useCallback((fromIndex: number, toIndex: number) => {
    setItems((prev) => arrayMove(prev, fromIndex, toIndex));
    rowRefs.current = arrayMove(rowRefs.current, fromIndex, toIndex);
  }, []);

  // 외부 value가 갱신돼도 기존 항목의 key(id)는 유지 → 불필요한 리마운트 방지
  useEffect(() => {
    setItems((prev) =>
      props.value.map((next, i) => ({
        ...next,
        key: prev[i]?.key ?? generateId(),
      })),
    );
  }, [props.value]);

  return (
    <DndContext onReorder={handleReorder}>
      {items.map((item, i) => (
        <Row key={item.key} value={item} ref={(el) => (rowRefs.current[i] = el)} />
      ))}
    </DndContext>
  );
});
```

#### 무한 스크롤 이력 타임라인의 로딩 상태를 명시적으로 모델링했습니다.

**2025.06 – 2025.07**

- 이슈의 담당자·상태·의견·결재 요청·조치 계획 변경 이력을 시간순으로 보여주는 컴포넌트, 이력 종류가 5가지이고 종류마다 "변경 전/후" 데이터 스키마가 완전히 다른 것이 난점
  - 단순히 "화면에 보이면 로드"하는 방식 대신, 최초 마운트(`isInitial`) · 로딩 중(`isLoading`) · 더 이상 데이터 없음(`isDone`) 세 상태를 조합해 `IntersectionObserver`의 observe/unobserve를 제어
  - 로딩 중 중복 요청이나, 데이터가 끝났는데도 observer가 남아 불필요하게 재호출되는 문제를 방지
  - 5가지 변경 유형별로 표시할 diff 내용이 서로 달라, 유형에 따라 렌더링을 분기하는 Content 컴포넌트를 설계
- 결재/조치계획 상세로 이동하는 링크는 해당 리소스가 아직 살아있을 때만 노출해야 하는 요구사항 (삭제된 리소스로의 dead link 방지)
  - `isRequestAlive`/`isRemediationAlive` 플래그로 조건부 렌더링해 해결

```tsx
function useInfiniteScrollObserver(
  onReachEnd: () => void,
  state: { isInitial: boolean; isLoading: boolean; isDone: boolean },
) {
  const targetRef = useRef<HTMLDivElement>(null);
  const [observe, unobserve] = useIntersectionObserver(onReachEnd);

  useEffect(() => {
    if (state.isInitial) return;

    // 로딩 중이거나 더 이상 데이터가 없으면 관찰을 멈추고,
    // 그 외에만(대기 상태) 스크롤 끝 지점을 관찰한다
    if (state.isDone || state.isLoading) {
      unobserve(targetRef.current!);
    } else {
      observe(targetRef.current!);
    }
  }, [state.isInitial, state.isDone, state.isLoading]);

  return targetRef;
}

// 변경 유형(5종)마다 다른 스키마를 공통 뷰로 렌더링
function TimelineEntry({ type, before, after }: TimelineEntryProps) {
  const Content = CONTENT_BY_TYPE[type];
  return <Content before={before} after={after} />;
}
```


#### 컴포넌트의 타입별 중복 구현을 컴파운드 컴포넌트 패턴으로 리팩토링했습니다.

**2025.02**

- 프로젝트·자산·작업·이슈태그 등 5가지 데이터 타입의 목록을 보여주는 테이블에서, 타입마다 레이아웃·페이지네이션·행 렌더링 로직이 통째로 중복된 독립 컴포넌트로 구현되어 공통 UI 하나를 고치려면 5곳을 각각 수정해야 하는 문제
  - `InformationTable`(타입에 따라 하위 Body를 스위칭하는 컨테이너) → `*InformationBody`(타입별 바디, 공통 `BodyWrapper`+`ResponsiveRow`+`Divider` 레이아웃 공유) → `*ListRow`(타입별 행, 실제 콘텐츠만 담당) 3단 계층으로 분리
  - 여러 타입에서 중복되던 `Suffix`, `TooltipParagraph` 등 UI 조각을 공통 컴포넌트로 추출
  - 순수 삭제 652줄 / 순수 추가 208줄 — 동일 기능을 유지하면서 중복 코드를 약 65% 제거
- 리팩토링 이후 pagination 버튼 UI, empty row 높이, 반응형 tooltip, 라이선스 관련 액션 버튼 등 신규 요구사항이 들어올 때마다 공통 계층(`InformationTableFooter`, `BodyWrapper`) 수정만으로 5개 타입 전체에 일괄 반영되는 구조로 개선

#### 디자인 시스템의 시각 회귀 테스트를 자동화했습니다.

**2025.09 – 진행 중**

- 130여 개 컴포넌트를 Storybook으로 문서화하고 있었지만, 컴포넌트가 늘어날 때마다 시각 회귀 테스트(Visual Regression Test) 대상 목록을 수작업으로 관리해야 하는 문제
  - Storybook의 story-id 생성 규칙(CSF `title` sanitize, PascalCase export 이름을 kebab-case로 변환하는 알고리즘 — `Over10` → `over-10` 같은 숫자·대문자 경계 처리 포함)을 내부 구현과 동일하게 리버스엔지니어링하여 재구현
  - `.stories.*` 파일을 정적 파싱해 BackstopJS 테스트 시나리오를 자동 생성하는 스크립트를 작성, 컴포넌트를 추가해도 별도 등록 없이 자동으로 회귀 테스트 커버리지에 포함되도록 개선
- BackstopJS 기반 시각 회귀 테스트 환경을 Docker로 격리 구축하고, GitLab CI에 `pull → build_image → open_server → reference → test → approve` 스테이지 파이프라인으로 구성
- 테스트 엔진을 Puppeteer에서 Playwright로 전환하여 안정성 개선
